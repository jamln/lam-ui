import type { App } from 'vue';
import LamButton from "./button";
import LamIcon from './icon';
import LamRate from './rate';
declare const _default: {
    install: (app: App) => void;
};
export default _default;
export { LamButton, LamIcon, LamRate, };
